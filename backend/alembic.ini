# Dev-CLI convenience only; the app runs migrations programmatically at boot
# (app.models.db.init_db) and never reads this file. Usage from backend/:
#   alembic revision -m "add xyz table"
# The DB url comes from app settings (env.py), not from this file.
[alembic]
script_location = app/migrations
